#include <stdio.h>

int main()
{
    printf("program to print an intger enter by user \n enter the intger you want to print");
    int a;
    scanf("%d", &a);
    printf("the intger you entered=%d", a);

    return 0;
}
