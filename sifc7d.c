#include <stdio.h>

int main()
{
    printf("FINDIND THE SIZE OF INTGER CHARACTER FLOAT DOUBLE IN BYTES USING C");
    int a;
    float f;
    char c;
    double d;
    printf("\n the size of int =%ld bytes", sizeof(a));
    printf("\n the size of float =%ld bytes", sizeof(f));
    printf("\n the size of char =%ld bytes", sizeof(c));
    printf("\nthe size of double =%ld bytes", sizeof(d));
    return 0;
}
